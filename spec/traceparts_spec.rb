require 'spec_helper'

describe Traceparts do
  it 'has a version number' do
    expect(Traceparts::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
